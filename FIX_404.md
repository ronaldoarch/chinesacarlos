# 🔧 Fix: Erro 404 no Registro/Login

## 🐛 Problema Identificado

O erro mostra que a requisição está indo para:
```
https://g04swwkwow0c8swkkwko04cc.agenciamidas.com/auth/register
```

Mas deveria ser:
```
https://g04swwkwow0c8swkkwko04cc.agenciamidas.com/api/auth/register
```

**Falta o `/api` no caminho!**

---

## ✅ Solução

### No Colify - Serviço Frontend

Configure a variável de ambiente:

```env
VITE_API_URL=https://g04swwkwow0c8swkkwko04cc.agenciamidas.com/api
```

**⚠️ IMPORTANTE:**
- Deve ter `/api` no final
- Use HTTPS (não HTTP)
- Use a URL real do seu backend

---

## 🔍 Como Descobrir a URL Correta

1. No Colify, vá no serviço do **Backend**
2. Procure por **"Public URL"** ou **"Domain"**
3. Copie a URL completa
4. **Adicione `/api` no final**
5. Configure no frontend como `VITE_API_URL`

**Exemplo:**
- URL do backend: `https://g04swwkwow0c8swkkwko04cc.agenciamidas.com`
- `VITE_API_URL`: `https://g04swwkwow0c8swkkwko04cc.agenciamidas.com/api`

---

## 🧪 Testar se Está Correto

Após configurar, abra o console do navegador (F12) e verifique:

1. Deve aparecer: `🔗 API Base URL: https://.../api`
2. As requisições devem ir para `/api/auth/register`
3. Não deve mais aparecer erro 404

---

## 📋 Checklist

- [ ] `VITE_API_URL` configurada no frontend
- [ ] URL tem `/api` no final
- [ ] URL usa HTTPS
- [ ] URL é a URL real do backend
- [ ] Frontend foi rebuild após mudar a variável

**⚠️ IMPORTANTE:** Se você mudar `VITE_API_URL`, precisa fazer **rebuild** do frontend!

---

## 🔄 Rebuild do Frontend

No Colify, após mudar `VITE_API_URL`:

1. Vá no serviço do Frontend
2. Clique em **"Redeploy"** ou **"Rebuild"**
3. Aguarde o build completar
4. Teste novamente

---

## 💡 Código Atualizado

O código agora adiciona `/api` automaticamente se você esquecer, mas é melhor configurar corretamente!
